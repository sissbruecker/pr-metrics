.idea
*.iml
node_modules

# Local Claude Code settings (per-developer, not shared)
.claude/settings.local.json
.playwright-cli
*.sqlite
*.sqlite-shm
*.sqlite-wal
.env

# Compiled single-file executable (bun run compile)
/pr-stats
/dist
# Transient build shard bun may leave behind on --compile
*.bun-build
